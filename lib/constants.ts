export const GENDERS = Object.freeze({
	male: 1,
	female: 2,
	neutral: 3,
});

export const REVALIDATE_INTERVAL = Object.freeze({
	session: 60 * 15,
	catalog: 60 * 20,
	notFound: 60 * 1,
	page: 60 * 60,
});